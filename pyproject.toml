[project]
name = "os-disk-scheduling"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ruff>=0.15.15",
    "setuptools>=82.0.1",
]

[dependency-groups]
dev = [
    "setuptools>=82.0.1",
]
